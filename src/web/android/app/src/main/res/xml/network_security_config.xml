<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic is HTTPS only. Cleartext stays permitted for the loopback
  addresses a developer machine uses (localhost over `adb reverse`, 10.0.2.2
  from the emulator) so a locally running API stays reachable.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
